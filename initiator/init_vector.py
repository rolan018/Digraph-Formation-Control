import numpy as np

def init_vector_for_line(size):
    line = np.random.uniform(low=min, high=max, size=size)
    pass

def random_velocity(min, max, size):
    return np.random.uniform(low=min, high=max, size=size)